import { ScrollArea } from '@/components/ui/scroll-area';

const UserDashboard = () => {
  return (
    <section className=' h-screen'>
      <nav>
        <ScrollArea className='w-72 '>lorem</ScrollArea>
      </nav>
    </section>
  );
};

export default UserDashboard;
